services:
  api1:
    image: andreluizsilvadev/rinha-fraude:v8
    container_name: api1
    #restart: unless-stopped

    mem_limit: 164m
    cpus: 0.45

    #environment:
     # GOGC: 300
      #GODEBUG: gctrace=1
      
  api2:
    image: andreluizsilvadev/rinha-fraude:v8
    container_name: api2
    #restart: unless-stopped

    mem_limit: 164m
    cpus: 0.45

    #environment:
     # GOGC: 300
      #GODEBUG: gctrace=1

  # nginx:
  #   image: nginx:alpine
  #   container_name: nginx

  #   #restart: unless-stopped
  #   mem_limit: 6m
  #   cpus: 0.08
  #   ports:
  #     - "9999:9999"
  #     - "6060:6060"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - api1
  #     - api2


  haproxy:
    image: haproxy:3.0-alpine
    container_name: haproxy

    ports:
      - "9999:9999"

    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro

    depends_on:
      - api1
      - api2

    mem_limit: 20m
    cpus: "0.1"
