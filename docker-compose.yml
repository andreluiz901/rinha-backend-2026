services:
  api1:
    image: andreluizsilvadev/rinha-fraude:latest
    container_name: api1
    #restart: unless-stopped

    mem_limit: 166m
    cpus: 0.46

    #environment:
     # GOGC: 300
      #GODEBUG: gctrace=1
      
  api2:
    image: andreluizsilvadev/rinha-fraude:latest
    container_name: api2
    #restart: unless-stopped

    mem_limit: 166m
    cpus: 0.46

    #environment:
     # GOGC: 300
      #GODEBUG: gctrace=1

  # nginx:
  #   image: nginx:alpine
  #   container_name: nginx

  #   #restart: unless-stopped
  #   mem_limit: 6m
  #   cpus: 0.08
  #   ports:
  #     - "9999:9999"
  #     - "6060:6060"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - api1
  #     - api2

  haproxy:
    image: haproxy:3.0-alpine
    container_name: haproxy

    ports:
      - "9999:9999"

    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro

    depends_on:
      - api1
      - api2

    mem_limit: 12m
    cpus: "0.1"
